Real Elite Bathrooms photography goes here, named exactly as referenced in
the codebase (grep for `label=` in ImageSlot usages, or `.jpg`/`.png` paths
under src/data and src/components/sections). Drop a file in at the expected
path and pass it as the `src` prop on the matching <ImageSlot> to swap the
placeholder for the real photo — no other code changes needed.
